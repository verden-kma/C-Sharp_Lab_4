<UserControl x:Class="Lab_1.Views.BirthdateAsker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:viewModels="clr-namespace:Lab_1.ViewModels">
    
    <UserControl.Resources>
        <viewModels:PeopleVM x:Key="People" />
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="40" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="5 5" DataContext="{Binding Source={StaticResource People}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="7*" />
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Text="List of people" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"/>
        <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" ItemsSource="{Binding Path=ViewList}" SelectedItem="{Binding Path=Selected}" 
                  AlternatingRowBackground="LightSlateGray" CanUserAddRows="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Customer's name" Binding="{Binding Path=Name}"/>
                
                <DataGridTextColumn Header="Customer's surname" Binding="{Binding Path=Surname}"/>
                
                <DataGridTextColumn Header="Customer's email" Binding="{Binding Path=Email}"/>
                
                <DataGridTemplateColumn Header="Birthday" SortMemberPath="Birthday" CanUserSort="True" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Birthday, StringFormat=d}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Path=Birthday}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                    
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="Age" Binding="{Binding Path=Age}" />
                
                <DataGridCheckBoxColumn Header="Is adult" Binding="{Binding Path=IsAdult}" IsReadOnly="True" IsThreeState="False"/>
                
                <DataGridCheckBoxColumn Header="Is birthday" Binding="{Binding Path=IsBirthday}" IsReadOnly="True" IsThreeState="False"/>
                
                <DataGridTextColumn Header="Western Zodiac" Binding="{Binding Path=SunSign}" IsReadOnly="True"/>
                
                <DataGridTextColumn Header="Chinese Zodiac" Binding="{Binding Path=ChineseSign}" IsReadOnly="True"/>
                
            </DataGrid.Columns>
            </DataGrid>
        
        <ItemsControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4">
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="FrameworkElement.Margin" Value="5" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.Items>
                <Button Style="{StaticResource ButtonStyle}" Content="Filter" />
                <Button Style="{StaticResource ButtonStyle}" Content="Remove" Command="{Binding Path=RemoveCommand}"/>
                <Button Style="{StaticResource ButtonStyle}" Content="Save" Command="{Binding Path=SaveCommand}"/>
            </ItemsControl.Items>
        </ItemsControl>
        
    </Grid>

</UserControl>